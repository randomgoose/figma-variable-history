# Export variables

You can export any previous commit as a CSS file. To export a commit, select it from the timeline and click **Export**. 

The plugin will compile Figma variables into CSS and this process may take a while if the number of variables is large.

![Export variables](/images/export-variables.png)