# Variable Timeline

The **Variables** tab, you can view the changes to each variable. Use the **Filter** dropdown to find changes to specific properties.

Hover a change record and click the arrow to visit the commit where the change was made.

<video src="/videos/variable-timeline.mp4" controls className="mt-8"/>
