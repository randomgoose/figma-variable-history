import { ArrowDownToLine } from 'lucide-react'

# Generate changelogs

The plugin can render a changelog in the Figma canvas. Go to **Commits**, hover the timeline and click <ArrowDownToLine className="inline-block" size={14}/> to generate a changelog.

![Repo info](/images/logging.png)